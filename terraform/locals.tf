# locals {
#   docker_install_script_path = "${path.module}/install_docker.sh"
# }


###
#COMMENT >> PLEASE READ ME
#local was no longer needed,
#Since installing Docker can be accomplished with a few commands, we can embed them directly.